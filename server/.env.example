PORT=5000
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/dbname
JWT_SECRET=your-jwt-secret

# Password reset email (Nodemailer) - only YOU set this once; app users just receive the email
# Gmail: use "Other (Custom name)" in App passwords, then paste the 16-char password here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=your-email@gmail.com

# Link in reset email (where your frontend reset-password page lives)
FRONTEND_URL=http://localhost:3000

# YouTube Data API (for /api/youtube/fetch). Get key from Google Cloud Console.
YOUTUBE_API_KEY=your-youtube-api-key

# Optional: comma-separated. Cron will fetch from these every 4 hours.
# YOUTUBE_CHANNEL_IDS=UCxxx,UCyyy
# RSS_FEED_URLS=https://techcrunch.com/feed/,https://feeds.feedburner.com/TechCrunch/
